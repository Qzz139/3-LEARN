# 稳定版基线

用户指定当前版本为稳定版，Git标签为 `stable-2026-09-18`，发布到 `main`。
后续快速版从该提交建立 `codex/fast-sorting` 分支。

保留用户本次修改的参数：

- 正式分拣红外原始阈值24mm，回位容差5cm，视觉修正范围±25°，目标连续丢失30帧后停止。
- 正式分拣平移段超时20秒、无进展超时10秒、停车等待上限20秒、平移后等待0.3秒。
- demo2红外原始阈值25mm；模型仍为普通 `yolo26m.pt`。
- 舵机映射ID1底盘侧600/1190、ID2抓夹侧1073固定。
- 正式分拣放置后松爪内收，转到下一预定搜索角度后外伸；球放右区、瓶放左区，各三个朝向。

单球和单瓶demo此前已完整实测通过。正式分拣前两轮在原3cm容差下停止，
本次修改后的参数尚未完成六件整轮实机确认；标签表示保留用户指定的稳定基线。
软件检查通过后发布；本次发布不执行机器人动作。
