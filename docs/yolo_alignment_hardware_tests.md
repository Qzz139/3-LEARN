# YOLO转向抓放实测（2026-09-18）

## 纯识别

当前EP原装相机720p画面显示网球在左侧。只读模式未发送运动或LED命令。
普通yolo26m识别sports ball，置信度0.945，框中心x≈321/1280，抓取轴默认x=640。
第一步建议SDK+5°左转，单帧推理耗时约0.38秒；模型加载和预热约28秒。
日志：`records/yolo_alignment/20260918/yolo-align-19700101T013209667248Z/`。

## 首次自动动作

ID2固定1073反馈1076，ID1外伸600反馈599，夹爪打开。
第1帧球中心321，左转+5°；第2帧中心398，继续左转+5°。
第3帧球中心120，与前一目标中心跳跃超过0.2画面比例，连续性匹配拒绝；
第4帧球中心59，第5帧无检测。三帧丢失后请求停止并亮红灯，不自动回位。
尚未夹紧、尚未放置；本轮不能标记为自动抓取成功。球是否实际移动待用户现场确认。
日志：`records/yolo_alignment/20260918/yolo-align-19700101T013301055321Z/`。
本地原图/标注图保存在`work/jetson-validation/`对应目录，未提交截图。

第一步指令+5°之后，yaw从6.04减少到0.87，实际姿态差−5.17°。
确认这台EP的姿态反馈符号与SDK转角指令相反，新增yaw_feedback_sign=-1，
将姿态差转换到指令角度方向。已验证跨±180°及启动基准右侧放置的计算。
当前25项软件测试通过；修正后的完整自动抓放仍待复测。

## 距离调整实测与修复

以下记录均为2026-09-18实测；Jetson系统时钟仍显示1970。

- `025929094701Z`：约1.37–1.40°小转角返回动作成功，但yaw几乎不变，现场确认完全不动。
  主动停止，未前进/夹紧。最低纠正角度改为此前实测有效的5°，新增实际yaw无进展检查。
- `030203012521Z`：5°指令实际左转约3.88°，球居中，红外142mm。
  首次速度指令后程序误报停止拒绝，现场确认未前进。诊断发现官方速度协议为PUSH、无需应答，
  SDK同步发送仍返回False；移除错误的返回值判定，改由实际里程判断进展。
- `030459678552Z`：0.02m/s、5mm一步，第一步实际里程约6.11mm，红外126→113mm；
  第二步无里程进展停止。现场确认第一步挪动、第二步不动，没有阻挡。
  后续前进速度改0.04m/s，用户建议加大步长，设置为1cm。
- `030737513365Z`：1cm步长版本尚未前进即因三帧丢失停止。原图下半幅花屏，球仍在；
  现场确认球未移动/遮挡。改为模型加载预热完成后才开启图传，分辨率720p→540p（传输原本已是TCP）。
- `030950801755Z`：540p持续检出网球，多步前进，红外119→91→55→32→21mm，
  最后一段看到≤20mm便请求停车。四个完整路段实际里程约23.14、20.39、16.69、20.24mm，
  明显大于请求10mm，仍需评估制动与里程误差。随后“0.1秒位置变化≤1mm”检查失败停止，未夹紧。
  用户反馈：在抓取范围内左右摇摆，最后停在完美抓取方位和范围。
  该判定已改为三次不同时间戳的新速度反馈确认接近零；此修复尚待实机验证。

以上五轮均未完成新demo完整抓放，不能标记为成功。日志目录均位于
`records/yolo_alignment/20260918/yolo-align-19700101T<上述编号>/`；原图仅保留在本地`work/jetson-validation/`。
当前37项软件测试通过。

## 连续取帧修复与抓放成功

- `031359145641Z`：仅540p和延后开启视频仍不足，首帧检出球后再次连续丢失，未夹紧。
- 检查Jetson安装的SDK：解码后的64帧队列满时会阻塞解码，压缩流32包队列满时会丢弃数据。
  增加持续后台取帧，防止舵机动作/推理期间无人取图引起队列积压；检测要求本次请求之后的新图像。
- `031725277648Z`：`--observe --observe-frames 10`，十帧全部检出网球，置信度约0.883–0.909，
  均在抓取轴容差内，没有运动或LED命令。视频首次连续识别验证通过。
- `031815924201Z`：从上一轮停下后的可抓位置重启。三帧居中，三次新红外采样确认，记录读数11mm。
  ID2定位后固定（反馈1074），ID1外伸599，夹紧、内收1182，右转到启动偏角−90.57°、外伸松爪放置，
  内收1183、返回朝向、外伸599松爪，程序完成无错误。
  用户现场确认确实夹住网球、放到右侧、正常返回初始态。

本轮从可抓位置开始，未前进，返回中心检查误差约2.24mm，但没有实际倒车路段。
因此成功确认的是视觉/红外放行与完整抓放；距离接近已单独出现有效多步运动，
新速度停车判定以及“接近→抓取→沿路径倒车→放置”仍需同一轮端到端验证。
实测仍有5°纠正引起的左右摆动，以及实际前进路段超过请求1cm的问题，后续需要进一步调节。
当前本地及Jetson均38项软件测试通过。
