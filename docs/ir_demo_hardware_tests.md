# 抓放 demo 实机调试记录（2026-09-18）

**映射更正：用户在交换测试后确认ID2驱动抓夹侧，ID1为底盘侧。此前文中及日志的
“抓夹侧/底盘侧”、distal/base标签来自写反的配置，不能据此认定物理关节；请以servo_id为准。
原始日志保留，9°至29°及552测试均实际作用于抓夹侧ID2。**

## 初始化与首次抓放

用户在 App 移除机械臂配置后，独立舵机命令可以执行。
初始化反馈：抓夹侧约605（目标601）、底盘侧1073，抓夹打开，底盘未转动。

首次完整测试收到17、19、19 mm三次红外反馈，按20 mm阈值触发夹紧。
用户确认夹住网球且没有碰撞。随后旧内收目标552未通过反馈验收，程序停止并请求红灯；
尚未右转或放置。停下后只读反馈约602/1049。

552来自参考模型推算，不是这台设备的实测内收位置，不应重试。
该值对应SDK角度−83°，而初始1073对应9°，差约92°。

## 小步确认方向

抓夹释放后，保持抓夹侧602，不再向它发送定位命令；底盘不转动。
底盘侧9°→14°两次到位，随后逐步19°→24°→29°，每步5°、停2秒，均通过反馈验收。

| SDK命令 | 预期DDS值 | 本次到位反馈 |
| --- | --- | --- |
| 9° | 1075 | 1073–1078 |
| 14° | 1104 | 1101 |
| 19° | 1132 | 1128 |
| 24° | 1161 | 1155 |
| 29° | 1189 | 1183 |

用户最终更正观察为：数值增加时**内收并下沉**，并非抬高。
用户进一步明确：**外伸必须是抓夹最低点，内收必须抬高，抓夹侧舵机保持固定。**
因此上述内收并下沉的动作不合格，不能以离地间隙足够为理由继续使用。
1073不能作为已确认的最低外伸目标；552及29°均不能作为有效内收目标。
配置中的外伸/内收目标已清空，不再自动执行这些姿态。所有动作测试停止；
先核对物理舵机对应关系及运动几何，再确定目标，尚未完成完整抓放周期。

日志保存于 `records/grasp_tests/20260918/`，包括初始化、首次失败及各次小步测试。
Jetson系统时间未同步，日志UTC显示1970年；日期按工作站记录。

## 官方数值依据

[Servo.moveto官方接口](https://robomaster-dev.readthedocs.io/en/latest/python_sdk/robomaster.html#module-robomaster.servo)
接收−180°至180°的整数绝对角度。
[官方SDK源码](https://github.com/dji-sdk/RoboMaster-SDK/blob/master/src/robomaster/servo.py)
将命令角度编码为 `(angle + 180) * 10`。此编码不是DDS反馈编码器值。

实机get_angle查询与DDS核对：564对应99.1°、1078对应189.4°，支持DDS每1024单位约180°。
初始定位及小步试动也验收了当前转换。官方接口通用数值范围不能替代机械装配的实际可达范围；
官网没有指定552为此装配姿态的内收目标。

## 恢复抓夹侧并单次测试底盘侧600

用户要求先将抓夹侧恢复到此前记录值，再查官方资料；如查不到姿态数值，测试底盘侧600。
抓夹侧目标601，SDK命令−74°，到位反馈602；底盘侧保持1184。
查阅官方舵机接口及机械臂模块说明，未找到上述固定单关节运动所需的外伸/内收原始目标值。
官方资料仅给出舵机命令角度范围及机械臂末端运动范围，不能据此确定目标编码器值。

随后仅向底盘侧ID2发送一次目标raw600（SDK−75°，预期DDS597），抓夹侧不再发送定位命令。
SDK动作报告成功，但五秒内反馈未到位，观测范围916–1184，最后953。
程序请求停止并切换红灯，不重试、不自动回位，没有抓夹开合或底盘转向。
现场运动方向、是否反复拉动尚待用户观察确认，不能把600标为有效姿态目标；配置仍保留null。
日志：`pose-probe-3595417100800/run.json`、`pose-probe-3638221164256/run.json`。

## 临时交换两个舵机角色

用户要求反过来测试。仅在独立探测进程中临时设为：抓夹侧候选ID2/槽1，
先恢复它自己的原记录1073后不再发送定位命令；底盘侧候选ID1/槽0，单次测试600。
开始前ID2反馈1105、ID1反馈601。恢复ID2后1075；ID1测试600后599；
结束时两路分别1075、599，反馈均通过验收。没有抓夹开合或底盘转向。
ID1原本就接近600，因此这次不能证明其完整运动方向或范围，也不能单凭到位证明物理映射。
第一段ID2恢复实际驱动哪个物理关节仍待用户确认；主配置中的物理角色尚未更改。
日志：`pose-probe-4108361738656/run.json`。

另一次只读查询发现，上一轮600失败后ID1仍602，ID2已回到1171–1172。
核对安装SDK及官方servo.py，异常路径的pause发送_enable=0，不能视为位置保持。
该回摆可能与禁用驱动有关，物理原因尚未证实；软件未再次发送29°目标。

## 用户确认物理关节，修正主配置

用户确认交换测试第一段ID2恢复动作驱动抓夹侧。修正为抓夹侧ID2/槽1/固定1073，
底盘侧ID1/槽0。此前将ID1设为抓夹侧、ID2设为底盘侧，是代码配置的物理角色映射错误。
该更正不等于已确定底盘侧运动范围；外伸/内收目标仍为null，不继续动作测试。

## 正确底盘侧ID1的小步回收方向测试

用户要求尝试收回。抓夹侧ID2保持1073目标（反馈1075），不再给它发定位命令。
仅向底盘侧ID1发送raw626/SDK−70°，开始反馈596，结束620，反馈在容差8内。
这一步约5°，尚待用户确认是否同时内收并抬高；不据此填写最内或最低外伸目标。
未开合夹爪、未转向、未自动回位。日志：`pose-probe-4278243290496/run.json`。

用户随后回复继续。仅对底盘侧ID1顺序发送raw654/−65°、683/−60°、711/−55°，
每步到位后停1.5秒，反馈分别649、677、706，结束707。抓夹侧ID2保持1075，
不再发送定位命令；夹爪和底盘均不动作。全部反馈在容差内，尚待用户确认高度及内收是否足够。
日志：`pose-probe-4356143597568/run.json`。这些数值不自动写成最内极限。

## 使用正确映射直接测试完整demo

用户确认问题就是两个角色写反，要求修改demo并直接实测。主配置采用抓夹侧ID2/槽1固定1073，
底盘侧ID1/槽0外伸600、内收711；两端点均已有到位反馈，尚不称作机械极限。
恢复红外20mm原始反馈连续三次触发，YOLO独立日志，执行单次抓取、右转90°放置及返回。

完整demo日志显示各舵机端点到位、右转−90°与返回+90°均完成，正常结束于外伸松爪。
用户明确确认实机抓取放置成功，同时认为ID1内收保守，要求加大到最大值。
本轮红外20、20、18mm触发；收回711反馈707，放置600反馈599，返回711反馈706，
最后外伸600反馈601。抓夹側ID2固定约1075，约束检查未报错。
日志：`ir-demo-19700101T011524615886Z/`。
YOLO在动作结束约2.4秒后才报告model_ready，旧退出逻辑未处理缓存帧，因此本轮无检测事件。
已修正为退出前处理一帧缓存图像，机械动作结束后最多等待30秒，16项软件测试通过。

## 扩大正确底盘侧内收范围

保持ID2固定1073/反馈1075，ID1先到已验证711，再按5°逐步到740、768、796、825、853、882。
各步反馈706、735、763、791、819、848、876，结束877，全部到位；尚待用户判断是否收尽。
日志：`pose-probe-4662197858400/run.json`。未开合夹爪、未转动底盘。

用户确认882后还可继续。ID1顺序测试910、939、967、996、1024、1052，
对应反馈905、934、962、991、1019、1048；结束ID1=1048、ID2=1075。
全部到位，用户正在判断是否收尽。日志：`pose-probe-4748963760096/run.json`。

用户确认1052后仍可继续。ID1依次1081、1109、1138、1166、1195到位，
反馈1076、1105、1133、1161、1190。1223/SDK35°在五秒内未完成，最后1167，
程序请求停止并亮红灯，不重试。用户明确反馈“到头了”。
因此采用最后成功到位的1195/SDK30°作为demo内收目标，外伸600、ID2固定1073；
不使用失败的1223目标。日志：`pose-probe-4828685209056/run.json`。

用户随后明确要求实际程序内收参数设为1190，已据此设置，替代1195候选。
1190换算SDK29°、预期DDS1189，仍按原始目标1190及容差8验收。

## 用户指定1190后的完整复测

本轮主配置ID2固定1073，ID1外伸600、内收1190。红外20、19、18触发夹紧；
内收两次反馈1183，右转−90°、放置松爪、返回+90°均完成，最终外伸598、夹爪松开。
程序正常完成，固定ID2检查未报错；用户现场回复“都正常”，确认实机抓放和返回成功。
YOLO于动作完成约0.4秒后model_ready，随后处理缓存帧，确有detections事件，count=0；
该帧没有检测到物体，不能据此声称本轮识别到网球。
日志：`ir-demo-19700101T012315008104Z/`。

据此将当前600/1190/1073配置记为已实机确认，calibration_verified=true；该字段仍仅用于提示。
未因这一记录更新重复驱动车辆。
